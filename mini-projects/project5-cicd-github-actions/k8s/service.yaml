apiVersion: v1
kind: Service
metadata:
  name: flask-tasks
spec:
  type: NodePort
  selector:
    app: flask-tasks
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30090
